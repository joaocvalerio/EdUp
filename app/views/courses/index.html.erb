<h1><%= current_user.company.name %> Courses</h1>

<% @courses.each do |course| %>
  <h2><%= course.name %></h2>
  <p><%= course.description%></p>
  <ol>
  <% course.parts.each do |part| %>
    <li><%= part.description %></li>
  <% end %>
  </ol>
<% end %>
