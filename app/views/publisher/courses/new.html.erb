<%= form_for [:publisher, @course] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.hidden_field :company_id,:value => current_user.company_id %>
  <%= f.submit %>
<% end %>

